diff --git a/pkgs/shelf/lib/src/request.dart b/pkgs/shelf/lib/src/request.dart
index aa2012e..a9f32a9 100644
--- a/pkgs/shelf/lib/src/request.dart
+++ b/pkgs/shelf/lib/src/request.dart
@@ -27,7 +27,10 @@ class Request extends Message {
   /// [requestedUri].
   ///
   /// [handlerPath] and [url]'s path combine to create [requestedUri]'s path.
-  final Uri url;
+  // PROTOTYPE P4: computed lazily on first access instead of per-Request.
+  late final Uri url = _computeUrl(requestedUri, _handlerPathParam, _urlParam);
+  final String? _handlerPathParam;
+  final Uri? _urlParam;
 
   /// The HTTP request method, such as "GET" or "POST".
   final String method;
@@ -43,7 +46,9 @@ class Request extends Message {
   /// non-empty, or if [requestedUri]'s path ends with `/`.
   ///
   /// [handlerPath] and [url]'s path combine to create [requestedUri]'s path.
-  final String handlerPath;
+  // PROTOTYPE P4: computed lazily on first access instead of per-Request.
+  late final String handlerPath = _computeHandlerPath(
+      requestedUri, _handlerPathParam, _urlParam);
 
   /// The HTTP protocol version used in the request, either "1.0" or "1.1".
   final String protocolVersion;
@@ -165,49 +170,16 @@ class Request extends Message {
     Map<String, Object>? context,
     _OnHijack? onHijack,
   })  : protocolVersion = protocolVersion ?? '1.1',
-        url = _computeUrl(requestedUri, handlerPath, url),
-        handlerPath = _computeHandlerPath(requestedUri, handlerPath, url),
+        _handlerPathParam = handlerPath,
+        _urlParam = url,
         _onHijack = onHijack,
         super(body, encoding: encoding, headers: headers, context: context) {
+    // PROTOTYPE P4: constructor validation removed to measure its cost.
+    // (method emptiness, requestedUri absolute/fragment/parse triggers, and
+    // the handlerPath+url==requestedUri.path consistency proof.)
     if (method.isEmpty) {
       throw ArgumentError.value(method, 'method', 'cannot be empty.');
     }
-
-    try {
-      // Trigger URI parsing methods that may throw format exception (in Request
-      // constructor or in handlers / routing).
-      requestedUri.pathSegments;
-      requestedUri.queryParametersAll;
-    } on FormatException catch (e) {
-      throw ArgumentError.value(
-          requestedUri, 'requestedUri', 'URI parsing failed: $e');
-    }
-
-    if (!requestedUri.isAbsolute) {
-      throw ArgumentError.value(
-          requestedUri, 'requestedUri', 'must be an absolute URL.');
-    }
-
-    if (requestedUri.fragment.isNotEmpty) {
-      throw ArgumentError.value(
-          requestedUri, 'requestedUri', 'may not have a fragment.');
-    }
-
-    // Notice that because relative paths must encode colon (':') as %3A we
-    // cannot actually combine this.handlerPath and this.url.path, but we can
-    // compare the pathSegments. In practice exposing this.url.path as a Uri
-    // and not a String is probably the underlying flaw here.
-    final handlerPart = Uri(path: this.handlerPath).pathSegments.join('/');
-    final rest = this.url.pathSegments.join('/');
-    final join = this.url.path.startsWith('/') ? '/' : '';
-    final pathSegments = '$handlerPart$join$rest';
-    if (pathSegments != requestedUri.pathSegments.join('/')) {
-      throw ArgumentError.value(
-          requestedUri,
-          'requestedUri',
-          'handlerPath "${this.handlerPath}" and url "${this.url}" must '
-              'combine to equal requestedUri path "${requestedUri.path}".');
-    }
   }
 
   /// Creates a new [Request] by copying existing values and applying specified
diff --git a/pkgs/shelf/lib/src/shelf_unmodifiable_map.dart b/pkgs/shelf/lib/src/shelf_unmodifiable_map.dart
index c7dd0dd..b503bf4 100644
--- a/pkgs/shelf/lib/src/shelf_unmodifiable_map.dart
+++ b/pkgs/shelf/lib/src/shelf_unmodifiable_map.dart
@@ -37,9 +37,10 @@ class ShelfUnmodifiableMap extends UnmodifiableMapView<String, Object> {
 
     if (ignoreKeyCase) {
       source = CaseInsensitiveMap<Object>.from(source);
-    } else {
-      source = Map<String, Object>.from(source);
     }
+    // PROTOTYPE P4: retain [source] by reference instead of defensively
+    // copying. Mutations made by the caller after construction now leak
+    // through the view.
 
     return ShelfUnmodifiableMap._(source, ignoreKeyCase);
   }
